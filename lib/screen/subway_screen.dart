import 'package:flutter/material.dart';
import 'package:transport/screen/serve/base_screen.dart';

class SubWayScreen extends StatelessWidget {
  const SubWayScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return BaseWidget(body: Text("지하철 화면"));
  }
}
